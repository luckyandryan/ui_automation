[pytest]
addopts =
    --verbose
    ; --html=./reports/test_results.html ;for HTML report generation
    ; --self-contained-html ;for HTML report generation
log_cli = 1
log_cli_level = INFO
log_cli_format = %(asctime)s - %(levelname)s - %(name)s - %(message)s
log_cli_date_format=%Y-%m-%d %H:%M:%S
